export default function Header(){
    const style = {
        marginBottom: "0.5em",
        display: "flex",
        alignItems: "center",
        justifyContent: "center" 
    }
    
    return (
        <header style={style}>
            Colour Input Displayer
        </header>
    )
}